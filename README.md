# dotfiles

These are my dotfiles.  
They're optimized to be used on Ubuntu 22.04 inside the WSL on a Windows host.

## Installation

Easily install the config using `git` and `stow`.

```bash
git clone https://github.com/luispflamminger/dotfiles.git ~/dotfiles
cd ~/dotfiles
./stow-all.sh
```
